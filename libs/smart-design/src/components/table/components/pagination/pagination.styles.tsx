import styled from 'styled-components';

import { spaceS } from '@smartcoorp/smart-design/tokens';

const Container = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  gap: ${spaceS};
`;
export const Styled = {
  Container,
};
